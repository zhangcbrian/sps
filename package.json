{
  "name": "sps",
  "description": "SPS (Spec, Plan, Ship) — structured specs co-located with your code",
  "private": true,
  "packageManager": "pnpm@10.29.3",
  "scripts": {
    "test": "vitest run",
    "test:watch": "vitest",
    "build": "turbo build",
    "lint": "eslint"
  },
  "devDependencies": {
    "tsx": "^4.21.0"
  }
}
